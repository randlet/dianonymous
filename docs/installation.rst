============
Installation
============

At the command line::

    $ easy_install dianonymous

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dianonymous
    $ pip install dianonymous